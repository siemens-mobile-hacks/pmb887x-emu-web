# Pinning for the qemu-pmb887x wasm build.
#
# The qemu source is the qemu submodule of the pmb887x-emu meta-repo,
# checked out at the repo root as pmb887x-emu/ (branch: wasm-patches —
# all former patches/*.patch are committed there on top of
# qemu-pmb887x master, plus the AFE + RTC-seed commits cherry-picked
# from the perk11/alula fork line).
#
# Revisions = the submodule pins of the latest pmb887x-emu master
# (https://github.com/siemens-mobile-hacks/pmb887x-emu @ d0f101b), plus the
# DSP/AFE fix from alula's qemu-pmb887x dsp-stuff branch (b31b98fe):
# master alone aborts every Siemens fullflash during L1 GSM frame handling
# (">>EXIT<< FILE: l1bbcsg / CepName: H_GSMFR" at ~25-30 s — the L1<->DSP
# handshake starves); the AFE (LLE+HLE) commit fixes s75/el71/c81.
#   qemu b31b98fe1e (2026-09-06 "pmb887x: hacky AFE (LLE+HLE) implementation")
#   bsp  e6e73d1    (2026-09-11 "board: LG boards read the RTC counter as
#                    packed calendar fields" — the [rtc] format = "calendar"
#                    the LG firmware needs, see patches/0033 / qemu fbc6b45c)
#
# Known upstream bug worked around by patches/bsp/*.patch: the BSP board
# configs define an RF peripheral of type "hd155153np" that the emulator's
# device table does not define (only the pmb6272 RF stub exists) —
# qemu aborts with 'hardware error: Unknown device: hd155153np' at board
# init. The patch re-points the two affected includes at the pmb6272 stub.
export QEMU_PMB887X_REPO="https://github.com/Azq2/qemu-pmb887x"
export QEMU_PMB887X_REV="debfa3d6f56859e1f60bcae7af125433f90f061b"
export QEMU_PMB887X_BRANCH="wasm-patches"
# the pinned qemu rev lives on a fork branch (not yet in origin/master)
export QEMU_PMB887X_ALT_REPO="https://github.com/alula/qemu-pmb887x"
export QEMU_PMB887X_ALT_BRANCH="dsp-stuff"

export PMB887X_BSP_REPO="https://github.com/Azq2/pmb887x-dev"
export PMB887X_BSP_REV="e6e73d147b3b3c6c61d88935a1c61ba9ffe7b70a"

# Toolchain versions (mirror qemu's tests/docker/dockerfiles/emsdk-wasm64-cross.docker)
export EMSDK_VERSION="4.0.10"
export ZLIB_VERSION="1.3.2"
export PIXMAN_VERSION="0.44.2"
export LIBFFI_VERSION="v3.5.2"
export GLIB_VERSION="2.84.0"
